apiVersion: v1
kind: Service
metadata:
  name: {{ include "mailman-db-exporter.fullname" . }}
  labels:
    {{- include "mailman-db-exporter.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "mailman-db-exporter.selectorLabels" . | nindent 4 }}
